# Development broker. Anonymous access is acceptable here only because the
# listener is bound to loopback by Compose and no real equipment is attached.
# Use mosquitto.production.conf for anything else.
persistence true
persistence_location /mosquitto/data/
listener 1883 0.0.0.0
allow_anonymous true
log_dest stdout
